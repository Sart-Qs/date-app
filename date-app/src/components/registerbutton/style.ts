import styled,{css} from 'styled-components'

export const Button = styled.button`
    cursor: pointer;
`;

export const CentertDiv = styled.div`
    position: absolute;
    left: 50%;
    transform: translate(-50%);
    margin-left: auto;
    margin-right: auto;
    margin-top: auto;
    margin-bottom: auto;
    top: 55%;
`;