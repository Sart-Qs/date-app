import React from "react";
import { User } from "../models/user";
import axios from "axios";

export class LoginService{
    private baseUrl = "/api";
    
    login(user: User){
        return 
    }
}